/*
    VARCHAR(SIZE)

    Một chuỗi có độ dài BIẾN ĐỔI (có thể chứa chữ cái, số và ký tự đặc biệt). 
    Tham số size chỉ định độ dài chuỗi tối đa tính bằng ký tự - có thể từ 0 đến 65535
*/

CREATE TABLE demo_varchar(
    NAME_PERSON VARCHAR(255)
);